# Copy to secrets.toml for local Streamlit runs, or paste into the
# Streamlit Community Cloud "Secrets" UI for deployment.
# Locally, the app also reads NAMINGNAMES_NEON_DB from ../13july2026/.env,
# so a local secrets.toml is optional.
NAMINGNAMES_NEON_DB = "postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
